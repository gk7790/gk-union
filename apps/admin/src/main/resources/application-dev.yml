server:
  port: ${SERVER_PORT:91}

spring:
  application:
    name: ${APP_NAME:gk-admin-dev}
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      pool-name: ${DB_POOL_NAME:gk-admin-dev-hikari}
      maximum-pool-size: ${DB_POOL_MAX_SIZE:10}
      minimum-idle: ${DB_POOL_MIN_IDLE:2}
      connection-timeout: ${DB_CONNECTION_TIMEOUT:30000}
      validation-timeout: ${DB_VALIDATION_TIMEOUT:5000}
      idle-timeout: ${DB_IDLE_TIMEOUT:600000}
      max-lifetime: ${DB_MAX_LIFETIME:1800000}

  data:
    redis:
      database: ${REDIS_DATABASE:11}
      host: ${REDIS_HOST:192.168.1.95}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
      timeout: ${REDIS_TIMEOUT:6000ms}
      connect-timeout: ${REDIS_CONNECT_TIMEOUT:6000ms}
      lettuce:
        pool:
          max-active: ${REDIS_POOL_MAX_ACTIVE:32}
          max-wait: ${REDIS_POOL_MAX_WAIT:1000ms}
          max-idle: ${REDIS_POOL_MAX_IDLE:8}
          min-idle: ${REDIS_POOL_MIN_IDLE:1}

  devtools:
    restart:
      enabled: false

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

gk:
  datasource:
    type: ${DB_TYPE:mysql}
  lifecycle-alert:
    enabled: ${LIFECYCLE_ALERT_ENABLED:true}
  openapi:
    payout:
      async-submit: true
  redis:
    open: true
  time-zone: UTC
  psp:
    callback:
      base-url: ${PSP_CALLBACK_BASE_URL:http://localhost:8082}
  quartz:
    thread-count: ${QUARTZ_THREAD_COUNT:4}

logging:
  level:
    org.mybatis.spring: ERROR
    org.apache.ibatis: ERROR
    com.mysql: ERROR
    com.gk: INFO
  file:
    name: ${LOG_FILE:logs/gk-admin-dev.log}
