<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gk.ledger.dao.LedgerBalanceDao">
    <select id="selectMerchantAvailableBalance" resultType="java.math.BigDecimal">
        SELECT COALESCE(b.balance, 0)
        FROM ledger_account a
        LEFT JOIN ledger_balance b
          ON b.tenant_id = a.tenant_id
         AND b.account_id = a.id
        WHERE a.tenant_id = #{tenantId}
          AND a.owner_type = #{ownerType}
          AND a.owner_id = #{merchantId}
          AND a.account_type = #{accountType}
          AND a.currency = #{currency}
        LIMIT 1
    </select>

    <update id="applyEntry">
        UPDATE ledger_balance
        SET balance = balance + #{balanceChange},
            debit_total = debit_total + #{debitAmount},
            credit_total = credit_total + #{creditAmount},
            last_entry_id = #{lastEntryId},
            last_journal_no = #{lastJournalNo},
            last_posted_at = #{postedAt},
            version = version + 1,
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE tenant_id = #{tenantId}
          AND account_id = #{accountId}
          AND (#{allowNegative} = 1 OR balance + #{balanceChange} &gt;= 0)
    </update>
</mapper>
