<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gk.platform.dao.SysUserDao">

	<select id="getList" resultType="com.gk.platform.entity.SysUserEntity">
		select t1.*, (select t2.name from sys_dept t2 where t2.id=t1.dept_id) deptName,
        (SELECT t3.NAME FROM sys_tenant t3 WHERE t3.id = t1.tenant_id) tenantName
			from sys_user t1 where t1.super_admin = 0
		<if test="username != null and username.trim() != ''">
			and t1.username like #{username}
		</if>
		<if test="deptId != null and deptId.trim() != ''">
			and t1.dept_id = #{deptId}
		</if>
        <if test="tenantId != null and tenantId.trim() != ''">
            and t1.tenant_id = #{tenantId}
        </if>
		<if test="gender != null and gender.trim() != ''">
			and t1.gender = #{gender}
		</if>
		<if test="selfId != null">
			and t1.id != #{selfId}
		</if>
		<if test="deptIdList != null">
			and t1.dept_id in
			<foreach item="id" collection="deptIdList" open="(" separator="," close=")">
				#{id}
			</foreach>
		</if>
        <if test="tenantIdList != null">
            and t1.tenant_id in
            <foreach item="id" collection="tenantIdList" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
	</select>

	<select id="getById" resultType="com.gk.platform.entity.SysUserEntity">
		select t1.*, (select t2.name from sys_dept t2 where t2.id=t1.dept_id) deptName from sys_user t1
			where t1.id = #{id}
	</select>

	<select id="getByUsername" resultType="com.gk.platform.entity.SysUserEntity">
		select * from sys_user where username = #{username}
	</select>

	<update id="updatePassword">
		update sys_user set password = #{newPassword} where id = #{id}
	</update>

	<select id="getCountByDeptId" resultType="int">
		select count(*) from sys_user where dept_id = #{value}
	</select>

	<select id="getUserIdListByDeptId" resultType="Long">
		select id from sys_user where dept_id in
			<foreach item="deptId" collection="list" open="(" separator="," close=")">
				#{deptId}
			</foreach>
	</select>
</mapper>