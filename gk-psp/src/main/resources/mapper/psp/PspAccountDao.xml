<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gk.psp.dao.PspAccountDao">
    <select id="selectCallbackAccount" resultType="com.gk.psp.callback.model.PspCallbackAccount">
        SELECT
            a.tenant_id AS tenantId,
            a.psp_id AS pspId,
            a.id AS pspAccountId,
            p.psp_code AS pspCode,
            a.api_secret AS apiSecret
        FROM psp_account a
        INNER JOIN psp_provider p ON p.id = a.psp_id
        WHERE a.psp_account_no = #{pspAccountNo}
          AND a.status = 1
          AND p.status = 1
        LIMIT 1
    </select>
</mapper>
